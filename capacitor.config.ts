import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.linkdrive.admin',
  appName: 'LA Admin',
  webDir: 'out',

  // server: {
  //   url: 'https://link-drive-admin.vercel.app',
  //   cleartext: false,
  // },
};

export default config;
